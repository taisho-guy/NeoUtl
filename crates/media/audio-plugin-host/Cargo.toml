[package]
name = "neoutl-audio-plugin-host"
version = "0.1.0"
edition = "2024"

[dependencies]
vst3-host = "0.9.0"
clack-host = { version = "0.1.1", features = ["libloading"] }
thiserror = "2"
serde = { version = "1", features = ["derive"] }
