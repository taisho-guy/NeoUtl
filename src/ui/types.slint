// src/ui/types.slint

export struct TimelineObject {
    id: int,
    start-frame: int,
    end-frame: int,
    kind: int,
    layer: int,
    label: string,
    selected: bool,
}

export struct LayerState {
    visible: bool,
    locked: bool,
}
